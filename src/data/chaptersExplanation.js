// 各个章节高频重点习题的专属解析与考点分析
export const customExplanations = {
  // 第1章 操作系统概述
  'chapter1-1': `【考点剖析】
本题考察操作系统的定义与核心功能定位。操作系统是整个计算机系统的“大管家”，它对上提供统一简易的接口，对下掌控裸机硬件资源。
【核心难点】
理解“系统软件”的本质，区分应用软件与系统软件的区别。操作系统不仅是程序的集合，更是一个软硬件资源的分配管理者。
【提分提示】
在阐述重要性时，应扣住“屏蔽硬件细节”、“提高资源利用率”以及“为用户提供统一接口”三个维度。`,

  'chapter1-2': `【考点剖析】
考察操作系统的演进过程与分类特征，特别是在“CPU利用率”和“人机交互”这对矛盾上的演化轨迹。
【关键对比】
- 单道批处理：克服人机矛盾，但CPU经常等I/O。
- 多道批处理：实现“并发”以提升CPU利用率，但无交互。
- 分时系统：引入“时间片分时”，完美解决人机交互问题。
- 实时系统：强调在“严格时限内”响应，高可靠性是核心。`,

  'chapter1-16': `【考点剖析】
微内核架构是操作系统设计中的经典模型，与传统的单体内核（Monolithic Kernel）形成对比。
【优缺点对比】
- 优点：核心态极其精简，文件系统和驱动等都在用户态以服务形式运行。一处崩溃（如网卡驱动挂掉）不影响整个系统，可靠性、安全性极高，极易扩充。
- 缺点：用户态服务进程之间通信（IPC）需要频繁在用户态和内核态之间进行上下文切换，导致性能开销较大。`,

  // 第2章 进程与线程
  'chapter2-3': `【考点剖析】
进程（Process）与程序（Program）的本质区别，以及为什么需要引入进程。
【核心考点】
- 程序：静态的，是存储在磁盘上的指令集合。
- 进程：动态的，是程序在数据集合上的一次运行活动，是分配资源和调度的基本单位。
- 引入原因：在多道并发执行环境下，程序顺序执行的封闭性和可再现性被打破。静态的程序概念无法描述“并发执行”中进程间的制约关系、动态状态和上下文切换，因而引入进程概念。`,

  'chapter2-4': `【考点剖析】
经典的三态模型（就绪、执行、阻塞）以及五态模型（创建、就绪、执行、阻塞、终止）的转动机理。
【极易混淆点】
- 进程只能**主动**地由“运行态”变为“阻塞态”（通过发出系统调用请求资源或等待事件）。
- 进程只能**被动**地由“阻塞态”变为“就绪态”（由外部事件如I/O完成或中断处理程序唤醒）。
- “运行态”变为“就绪态”是因为时间片用完或有更高优先级的进程抢占。`,

  // 第3章 进程同步
  'chapter3-1': `【考点剖析】
同步与互斥的本质制约关系。
【解题关键】
- 互斥（间接制约关系）：源于对“共享临界资源”的争夺，多进程不可同时使用（如写同一片内存）。
- 同步（直接制约关系）：源于进程间的“协作”，即进程 A 的下一步必须依赖进程 B 完成了某项操作之后才能继续（如生产者/消费者）。`,

  'chapter3-2': `【考点剖析】
信号量及其 PV 操作的物理逻辑，这是多道程序设计中解决并发控制的基础。
【得分守恒律】
- P操作（Wait）：执行 <code>S = S - 1</code>。若 <code>S < 0</code>，表示当前资源不足，当前进程必须阻塞并挂入等待队列。
- V操作（Signal）：执行 <code>S = S + 1</code>。若 <code>S &le; 0</code>，说明等待队列中仍有被阻塞的进程，系统将主动唤醒该队列中的一个进程并使其进入就绪队列。`,

  // 第6章 内存管理
  'chapter6-14': `【考点剖析】
基本分页存储管理与基本分段存储管理的本质区别，是高频考点。
【核心对比】
1. 目的不同：分页是出于系统管理的需要，解决内存碎片，提高利用率，是系统物理行为；分段是为了满足用户编程、共享和保护的需要，是逻辑行为。
2. 长度不同：页的大小是固定且由系统决定的；段的长度是不固定的，由用户编写的源程序决定。
3. 二维性：分页的地址空间是一维的（只需给出一个逻辑地址，硬件即可自动算出页号和页内偏移）；分段的地址空间是二维的（必须给出段名和段内偏移）。`,

  // 第7章 虚拟存储管理
  'chapter7-6': `【考点剖析】
Belady 异常与虚拟存储的页面置换算法特性。
【核心机制】
- Belady 异常是指在使用 **FIFO** 页面置换算法时，分配给进程的物理块数增加，其缺页次数反而上升的奇异现象。
- 产生原因：FIFO 算法没有遵循进程最近访问模式的局部性，淘汰了最先进入的页面，而该页面可能是接下来最常访问的。
- 提示：**LRU** 和 **OPT** 算法不会产生 Belady 异常，因为它们满足“栈式算法”的递推性质。`
}

/**
 * 动态为题库生成通用的解析说明，用于未手写专属解析的题目
 */
export function generateFallbackExplanation(qId, question, answer) {
  const keyTerms = []
  const keywords = ['进程', '线程', '内存', '死锁', '算法', '设备', '文件', '接口', '调用', '并发', '互斥', '同步']
  keywords.forEach(word => {
    if (question.includes(word) || answer.includes(word)) {
      keyTerms.push(word)
    }
  })
  
  const termsStr = keyTerms.length > 0 ? keyTerms.join('、') : '操作系统核心概念'
  
  return `【考点剖析】
本题核心考察了关于 [${termsStr}] 的设计逻辑与底层工作原理。结合题干，需要明确其定义界定、触发条件以及与其他相关硬件或系统组件的交互机制。

【解题思路】
1. 理清问题本质：本题问的是 “${question.substring(0, 40)}${question.length > 40 ? '...' : ''}”；
2. 提取核心答题要点：答题时应从其基本定义出发，理顺状态机转换或设计目标，并按照系统管理的五大职能（处理机、存储器、设备、文件、接口）将其分点分类作答，确保逻辑的严密性与完整性。

【名师点拨】
在考场上遇到此类简答题时，注意一定要分条理阐述（如：一、定义；二、特征；三、作用）。答出关键术语（如“系统调用”、“上下文切换”、“PCB”等）是得分的关键。`
}
